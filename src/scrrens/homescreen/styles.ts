import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  listcontainer: {
    //alignItems: 'center',
    // fli
  },
});

export default styles;
